import { AgeToTextPipe } from './age-to-text.pipe';

describe('AgeToTextPipe', () => {
  it('create an instance', () => {
    const pipe = new AgeToTextPipe();
    expect(pipe).toBeTruthy();
  });
});
